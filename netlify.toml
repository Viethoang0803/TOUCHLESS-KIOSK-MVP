[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — Netlify serves static files in dist/assets/ before applying redirects
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
